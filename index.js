const main = () => {
  let game = generateRandomArray();

  console.log("sss", isSolvable(game.gridArray, rowLength ** 2));

  grid = game.gridArray;
  generateGrid(game.gridArray);
  eventListener();
};
